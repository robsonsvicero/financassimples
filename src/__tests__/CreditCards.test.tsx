import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import App from '../App';

describe('Fluxo de cartões', () => {
  it('permite adicionar um cartão (mock)', async () => {
    // Simule o login e navegue até a tela de cartões
    // Preencha o formulário e envie
    // Verifique se o cartão aparece na lista
  });

  it('permite editar e remover cartão (mock)', async () => {
    // Simule o login, adicione um cartão, edite e remova
    // Verifique se as ações refletem na UI
  });
});

// Adicione outros testes para cartões, categorias, orçamento, etc.
